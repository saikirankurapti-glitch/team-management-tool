# Meetings Workspace & Action Item Engine (`MeetingRecord`)

## 1. Meeting Templates
Pre-built templates for *Daily Standup*, *Sprint Planning*, *Sprint Review*, and *Retrospective*.

## 2. Meeting Action Item ➔ Task Conversion (`POST /api/v1/meetings/:id/convert-action`)
Extracts action items from meeting notes and converts them directly into assigned Work Items.
