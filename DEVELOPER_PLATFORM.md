# Developer Platform Specifications (`/settings/developer-apps`)

## 1. Developer Portal Overview
The Developer Portal allows organization administrators to:
- Register third-party OAuth applications (`OAuthApp`).
- Rotate OAuth client secrets securely.
- Configure granular permission scopes (`projects:read`, `work_items:write`, `analytics:read`).
- Dispatch test payload events to customer webhook endpoints.
