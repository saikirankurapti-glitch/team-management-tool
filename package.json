{
  "name": "startup-team-management",
  "version": "1.0.0",
  "private": true,
  "description": "Production-quality Startup Team Management & Collaboration Platform",
  "scripts": {
    "setup": "npm install && npm run install:server && npm run install:client && npm run db:setup",
    "install:server": "cd server && npm install",
    "install:client": "cd client && npm install",
    "db:setup": "cd server && npx prisma db push && npx ts-node seed.ts",
    "db:seed": "cd server && npx ts-node seed.ts",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "npm run build:server && npm run build:client",
    "build:server": "cd server && npm run build",
    "build:client": "cd client && npm run build",
    "test": "cd server && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prisma": "^5.22.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}
